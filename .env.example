# ========================================
# Photobooth Server Configuration
# Untuk Synology Deployment
# ========================================

# Server Configuration
NODE_ENV=production
PORT=3000

# Network Configuration
# Untuk Docker/Container: gunakan 0.0.0.0 agar accessible dari luar container
# Untuk development lokal: gunakan IP lokal Anda
LOCAL_IP=0.0.0.0

# Admin Authentication
# ⚠️ PENTING: Ganti password ini di production!
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Session Configuration
SESSION_SECRET=ganti-dengan-string-random-yang-panjang-dan-aman
SESSION_MAX_AGE=86400000

# Watch Folder - Source folder untuk monitoring file baru
# Untuk development lokal (macOS/Windows):
# macOS: LUMA_PHOTOS_FOLDER=/Users/username/LumaBooth/Photos/
# Windows: LUMA_PHOTOS_FOLDER=C:\\LumaBooth\\Photos\\
#
# Untuk Docker/Synology:
# Set ke path di dalam container yang di-mount dari Synology
# Contoh: LUMA_PHOTOS_FOLDER=/watch
# Lalu mount volume: /volume1/photo/camera -> /watch
#
# Jika tidak diset, akan fallback ke /app/test-photos
LUMA_PHOTOS_FOLDER=/app/watch

# Cleanup Configuration
# Auto cleanup galleries setelah X hari (0 untuk disable)
CLEANUP_AFTER_DAYS=7
CLEANUP_DAYS=7
# Jalankan cleanup setiap hari jam 2 pagi
CLEANUP_CRON=0 2 * * *

# QR Code Configuration
# ⚠️ PENTING: Ganti dengan IP Synology Anda!
# Contoh: http://192.168.1.100:3000 atau http://yourdomain.com
QR_BASE_URL=http://192.168.1.100:3000

# File Upload Configuration
MAX_FILE_SIZE=10485760
ALLOWED_EXTENSIONS=jpg,jpeg,png,gif,webp

# Thumbnail Configuration
THUMBNAIL_WIDTH=400
THUMBNAIL_HEIGHT=400
THUMBNAIL_QUALITY=80

# Database Configuration
# Aplikasi ini menggunakan SQLite (better-sqlite3)
# Database file akan dibuat di: /app/database/photobooth.db
# Tidak perlu konfigurasi MySQL/MariaDB

# CORS Configuration (optional)
# Uncomment jika perlu restrict CORS
# CORS_ORIGIN=http://localhost:3000

# Logging
LOG_LEVEL=info

# Development Only (tidak perlu di production)
# NODE_OPTIONS=--inspect
