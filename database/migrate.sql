-- Database Migration Script
-- Run this to update existing database schema

-- Version 1.0.0 to 1.0.1 (if needed in future)
-- Example migration:

-- Add new column to sessions table (example)
-- ALTER TABLE sessions ADD COLUMN customer_name TEXT;

-- Add new column to photos table (example)
-- ALTER TABLE photos ADD COLUMN processed BOOLEAN DEFAULT 0;

-- Create new indexes (example)
-- CREATE INDEX IF NOT EXISTS idx_sessions_customer ON sessions(customer_name);

-- For now, this file is a placeholder for future migrations
-- Current schema is in schema.sql
